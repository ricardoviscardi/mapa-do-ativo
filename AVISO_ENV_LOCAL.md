# .env.local incluído para teste local

Esta versão inclui o arquivo `.env.local` para facilitar o teste local.

Em produção, o recomendado continua sendo configurar `BRAPI_API_TOKEN` nas variáveis de ambiente da hospedagem.

O arquivo `.gitignore` já mantém `.env.local` fora do GitHub.
